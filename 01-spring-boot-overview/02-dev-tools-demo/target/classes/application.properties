spring.application.name=mycoolapp

#expose individual endpoints with comma
#management.endpoints.web.exposure.include=health,info

#Exclude individual endpoints with comma
#management.endpoints.web.exposure.exclude=health,info

#use wildcard to expose all endpoints
management.endpoints.web.exposure.include=*
management.info.env.enabled=true
info.app.name = My Super Cool App
info.app.description = A Crazy fun app, yohoo!
info.app.version=1.0.0

#Injecting custom app properties
custom.team="Hello Swetha"

#change spring boot embedded server port
server.port= 8080

#set application context path(all requests should be prefixed with mycoolapp)
server.servlet.context-path =/mycoolapp